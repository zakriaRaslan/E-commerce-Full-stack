import { OpenProductsBySubcategoryDirective } from './open-products-by-subcategory.directive';

describe('OpenProductsBySubcategoryDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenProductsBySubcategoryDirective();
    expect(directive).toBeTruthy();
  });
});
